import React, { Component } from 'react'
import { Grid, Typography } from '@material-ui/core';

export class Form extends Component {
  render () {
    return (
      <Grid>
        <Typography variant='h1'>Add Lead Form</Typography>
      </Grid>
    )
  }
}

export default Form
